  name: mixvy
  description: "A new Flutter project."
  publish_to: 'none'
  version: 1.0.0+1

  environment:
    sdk: ^3.11.1

  dependencies:
    flutter:
      sdk: flutter
    flutter_web_plugins:
      sdk: flutter
    flutter_localizations:
      sdk: flutter
    cloud_functions: ^6.0.4
    cupertino_icons: ^1.0.8
    go_router: ^17.1.0
    agora_rtc_engine: ^6.5.3
    uuid: ^4.3.0
    image_picker: ^1.0.7
    image: ^4.0.0
    collection: ^1.19.1
    firebase_auth: ^6.2.0
    connectivity_plus: ^7.0.0
    firebase_core: ^4.5.0
    firebase_messaging: ^16.0.3
    firebase_storage: ^13.0.3
    path_provider: ^2.1.2
    cloud_firestore: ^6.1.3
    firebase_analytics: ^12.1.3
    # supabase_flutter: ^2.5.6  # Removed legacy dependency
    flutter_stripe: ^12.4.0
    flutter_riverpod: 2.4.0
    freezed_annotation: ^3.1.0
    http: ^1.1.0
    flutter_dotenv: ^6.0.0
    share_plus: ^12.0.1
    firebase_crashlytics: ^5.0.8
    url_launcher: ^6.3.2
    shared_preferences: ^2.5.3
    google_sign_in: ^7.2.0
    logging: ^1.2.0
    package_info_plus: ^9.0.0
    patrol: ^4.3.0
    path: ^1.9.1
    # state_notifier has been removed

  dev_dependencies:
    flutter_test:
      sdk: flutter
    integration_test:
      sdk: flutter
    flutter_lints: ^6.0.0
    build_runner: ^2.13.1
    freezed: ^3.2.5
    json_serializable: ^6.13.1

    mocktail: ^1.0.0
    fake_cloud_firestore: ^4.1.0+1


  flutter:
    assets:
      - assets/images/
      - assets/icons/
      - assets/fonts/
      - assets/env/app_env
      - assets/.env
    uses-material-design: true

