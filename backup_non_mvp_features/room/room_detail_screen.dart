// Placeholder RoomDetailScreen widget. Replace with real implementation.
import 'package:flutter/material.dart';

class RoomDetailScreen extends StatelessWidget {
	const RoomDetailScreen({Key? key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Scaffold(
			appBar: AppBar(
				title: const Text('Room Detail'),
			),
			body: const Center(
				child: Text('Room detail screen placeholder.'),
			),
		);
	}
}