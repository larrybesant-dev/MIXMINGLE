// Placeholder PresenceWidget. Replace with real implementation.
import 'package:flutter/material.dart';

class PresenceWidget extends StatelessWidget {
	const PresenceWidget({Key? key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return const Text('Presence widget placeholder');
	}
}