{
  "name": "mixmingle-security-rules-tests",
  "version": "1.0.0",
  "description": "Firestore security rules tests for Mix & Mingle",
  "private": true,
  "scripts": {
    "test:rules": "jest firestore.rules.test.js --testTimeout=10000",
    "test:rules:watch": "jest firestore.rules.test.js --watch"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000
  }
}
