import 'package:flutter/material.dart';

class MutualFollowersRow extends StatelessWidget {
  const MutualFollowersRow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
