import 'package:flutter/material.dart';

class DjPanel extends StatelessWidget {
  const DjPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
