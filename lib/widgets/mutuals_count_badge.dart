import 'package:flutter/material.dart';

class MutualsCountBadge extends StatelessWidget {
  final int count;
  const MutualsCountBadge({Key? key, this.count = 0}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
