import 'package:flutter/material.dart';

class HostControlsOverlay extends StatelessWidget {
  const HostControlsOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
