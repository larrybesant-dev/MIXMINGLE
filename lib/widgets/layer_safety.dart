import 'package:flutter/material.dart';

class LayerSafety extends StatelessWidget {
  const LayerSafety({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
