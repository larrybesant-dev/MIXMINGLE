import 'package:flutter/material.dart';

class HostControlsWidget extends StatelessWidget {
  const HostControlsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
