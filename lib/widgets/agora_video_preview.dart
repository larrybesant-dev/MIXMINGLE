import 'package:flutter/material.dart';

class AgoraVideoPreview extends StatelessWidget {
  const AgoraVideoPreview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
