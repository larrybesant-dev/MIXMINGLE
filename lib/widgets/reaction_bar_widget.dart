import 'package:flutter/material.dart';

class ReactionBarWidget extends StatelessWidget {
  const ReactionBarWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
