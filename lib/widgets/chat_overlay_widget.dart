import 'package:flutter/material.dart';

class ChatOverlayWidget extends StatelessWidget {
  const ChatOverlayWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
