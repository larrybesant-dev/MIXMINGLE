import 'package:flutter/material.dart';

/// Lightweight global placeholders to satisfy references during compilation.
/// Replace with real implementations or remove when wiring real data.
String? avatarUrl = null;
String? effectiveUserId = null;
String? displayName = null;
